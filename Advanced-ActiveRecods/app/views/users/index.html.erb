<h1>Users#index</h1>
<p>Find me in app/views/users/index.html.erb</p>

<% @user.each do |user| %>
 <br>
 <%= user.name %> <%= link_to "new note", new_user_note_path(user) %>
 <%= link_to "All notes", user_note_path(user_id: user, id: 'all') %>
 <%#= link_to 'delete', user_path(id: user.id), method: :delete %>
<% end %>
